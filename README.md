# 현장 유류대 대조표 생성기 · 점검기 · 자재 거래명세서 정리기

건설 현장의 **장비작업일보**와 **경유사용집계표** 엑셀로 유류대 및 장비비 대조표를 만들고 점검하는 웹페이지입니다.
엑셀 파일은 브라우저 안에서만 읽으며 서버로 전송하지 않습니다.

| 페이지 | 하는 일 |
|---|---|
| `index.html` — 유류대 대조표 생성기 | 작업일보 + 경유사용집계표 → 월별 대조표, 장비별 평균(월별 유류사용량·시간당 사용량), 그래프가 든 엑셀 생성 |
| `checker.html` — 현장 유류대 점검기 | 완성된 정리 엑셀의 수식·숫자 불일치, 이상 주유, 누락 장비 점검 |
| `materials.html` — 자재 거래명세서 정리기 | 거래명세서(엑셀·글자 PDF) → 일별·월별·거래처별 사용금액, 재사용/소모성 분류, 자재관리대장 엑셀 |

## 사용법
1. 페이지를 열고 엑셀(.xlsx)을 끌어다 놓습니다.
2. 결과를 확인한 뒤 **엑셀 만들기 / 결과 엑셀로 받기** 버튼으로 내려받습니다.

## 읽는 양식
- 경유사용집계표: A열에 `장비명 / 회사 / 규격 / 성명 / 번호 / 날짜` 머리행, 아래로 일자별 수량·금액, `소 계` 행
- 장비작업일보: `차량번호` 머리행, 1~15일 / 16~31일 두 줄, `수 량(시간)`, `단가`, `금 액`, `비 고`(예: "4월분")
- 시트 이름이나 A1 칸에서 달(예: `26.8월`)을 읽습니다.

- 거래명세서: `품명(품목) · 규격 · 단위 · 수량 · 단가 · 공급가액(금액) · 세액 · 비고` 머리행이 있는 표. 일자는 `일자` 칸, `월/일` 칸, 또는 명세서 위쪽 날짜에서 읽고, 거래처는 공급자 `상호`에서 읽습니다.
- 스캔 PDF·사진은 이 GitHub 페이지에서는 읽지 못합니다 (claude.ai에 게시된 페이지에서는 Claude가 읽습니다).
- 자재 거래명세서 정리기로 받은 자재관리대장 엑셀을 다시 올리면 `품목 내역` 시트를 읽어 이어서 관리합니다.

## 사용한 라이브러리 (CDN)
SheetJS 0.18.5, ExcelJS 4.4.0, JSZip 3.10.1, pdf.js 3.11.174 (한글 CMap 일부는 페이지에 포함)
